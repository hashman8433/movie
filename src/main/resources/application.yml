spring:
  datasource:
    url: jdbc:mysql://localhost:3307/movie?autoReconnect=true&useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=CONVERT_TO_NULL&useSSL=false&serverTimezone=CTT
    username: root
    password: root
    filters: wall,mergeStat
    driver-class-name: com.mysql.cj.jdbc.Driver

    # 配置 mybatis
mybatis:
  type-aliases-package: com.yixuan.pojo
  mapper-locations: classpath:mybatis/mapper/user/*.xml

